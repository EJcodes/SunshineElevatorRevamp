import React from 'react'
import {SidebarContainer, Icon, ClosedIcon, } from './SidebarElements'


const Sidebar = () => {
    return (
        <SidebarContainer>
            <Icon>
                <CloseIcon />
            </Icon>
        </SidebarContainer>
    );
};

export default Sidebar;
